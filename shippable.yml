
build:
  pre_ci_boot:
    image_name: drydock/u16
    image_tag: master
    pull: true

  ci:
    - aws --version
    - az --version
    - terraform --version
    - kubectl version || true
    - gcloud --version
    - eb --version
    - packer --version
    - docker pull alpine
    - docker images
  #  - docker tag alpine gcr.io/sunlit-pixel-182606/alpine
  #  - gcloud docker -- push gcr.io/sunlit-pixel-182606/alpine
  #  - docker pull 374168611083.dkr.ecr.us-east-1.amazonaws.com/api
integrations:                               
  #hub:
  #  - integrationName: aws-int    #dummy commit
  #    type: ecr
  #    region: us-east-1
  #hub:
  #  - integrationName: gcr    #dummy commit
  #    type: gcr
