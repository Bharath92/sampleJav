
build:
  pre_ci_boot:
    image_name: drydock/u16
    image_tag: master
    pull: true

  ci:
    - aws --version
    - az --version
    - terraform --version
    - kubectl version || true
    - gcloud --version
    - eb --version
    - packer --version
