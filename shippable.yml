
build:
  #pre_ci_boot:
  #  image_name: drydock/u16
  #  image_tag: master
  #  pull: true

  ci:
    - aws --version
    - az --version
    - terraform --version
    - kubectl version || true
    - gcloud --version
    - eb --version
    - packer --version
    - docker pull alpine
    - docker images
    - docker tag alpine gcr.io/Google-cloud-POC/alpine
    - gcloud docker -- push gcr.io/Google-cloud-POC/alpine
  #  - docker pull 374168611083.dkr.ecr.us-east-1.amazonaws.com/api
integrations:                               
  #hub:
  #  - integrationName: aws-int    #dummy commit
  #    type: ecr
  #    region: us-east-1
  hub:
    - integrationName: gcr    #dummy commit
      type: gcr
